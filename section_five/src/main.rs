

mod lessons;

use lessons::expressions::expressions;
use lessons::ownership::ownership;
use lessons::funcionality::funcionality;

fn main() {
    
    // Expressions
    expressions();
    
    // Ownership
    ownership();
    
    // Functionality
    funcionality();
}
